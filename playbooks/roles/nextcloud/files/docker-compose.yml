---
services:
  app:
    image: nextcloud:32.0.4-fpm-alpine
    restart: always
    env_file: .env
    volumes:
      - nextcloud-data:/var/www/html
    depends_on:
      - db
      - redis
  caddy:
    image: caddy:2.10.2-alpine
    restart: always
    volumes:
      - nextcloud-data:/var/www/html:ro
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
    ports:
      - 3004:80
  db:
    image: postgres:18.1-alpine
    restart: always
    env_file: .env
    ports:
      - 127.0.0.1:6902:5432
    volumes:
      - nextcloud-postgres:/var/lib/postgresql
  redis:
    image: valkey/valkey:9.0.1-alpine
    restart: always

volumes:
  nextcloud-data:
    driver: local
  nextcloud-postgres:
    driver: local
